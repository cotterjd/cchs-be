import GQLDate from '../types/gqlDate'
import { v4 as uuid } from 'uuid'

// Functionality is in the controller folder, Resolvers are not defined atm. 
export default {
  DateTime: GQLDate,
  Query: {},
  Mutation: {},
}

